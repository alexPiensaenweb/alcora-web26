---
import AdminLayout from "../../layouts/AdminLayout.astro";
import { directusAdmin } from "../../lib/directus";

const url = new URL(Astro.request.url);
const estadoFilter = url.searchParams.get("estado") || "";
const tipoFilter = url.searchParams.get("tipo") || "";
const page = parseInt(url.searchParams.get("page") || "1");
const limit = 20;
const offset = (page - 1) * limit;

let pedidos: any[] = [];
let total = 0;

try {
  let filters = "";
  if (estadoFilter) filters += `&filter[estado][_eq]=${estadoFilter}`;
  if (tipoFilter) filters += `&filter[tipo][_eq]=${tipoFilter}`;

  const res = await directusAdmin(
    `/items/pedidos?sort=-date_created&limit=${limit}&offset=${offset}&fields=id,tipo,estado,date_created,total,subtotal,costo_envio,metodo_pago,user_created.id,user_created.first_name,user_created.last_name,user_created.razon_social,user_created.email,user_created.telefono${filters}&meta=filter_count`
  );
  pedidos = res.data || [];
  total = res.meta?.filter_count || 0;
} catch (err) {
  console.error("[admin/pedidos] Error:", err);
}

const totalPages = Math.ceil(total / limit);

const TIPOS = [
  { value: "", label: "Todos" },
  { value: "pedido", label: "Pedidos" },
  { value: "presupuesto", label: "Presupuestos" },
];

const ESTADOS = [
  { value: "", label: "Todos" },
  { value: "solicitado", label: "Solicitados" },
  { value: "presupuesto_solicitado", label: "Ppto. solicitado" },
  { value: "aprobado_pendiente_pago", label: "Pendiente pago" },
  { value: "pagado", label: "Pagados" },
  { value: "enviado", label: "Enviados" },
  { value: "cancelado", label: "Cancelados" },
];

const estadoLabelMap: Record<string, string> = {
  solicitado: "Solicitado",
  presupuesto_solicitado: "Presupuesto",
  aprobado_pendiente_pago: "Pte. pago",
  pagado: "Pagado",
  enviado: "Enviado",
  cancelado: "Cancelado",
};

const estadoColorMap: Record<string, string> = {
  solicitado: "bg-yellow-100 text-yellow-800",
  presupuesto_solicitado: "bg-amber-100 text-amber-800",
  aprobado_pendiente_pago: "bg-blue-100 text-blue-800",
  pagado: "bg-green-100 text-green-800",
  enviado: "bg-emerald-100 text-emerald-800",
  cancelado: "bg-red-100 text-red-800",
};

function formatDate(d: string) {
  return new Date(d).toLocaleDateString("es-ES", { day: "2-digit", month: "short", year: "numeric" });
}

function clienteNombre(p: any) {
  const u = p.user_created;
  if (!u) return "—";
  return u.razon_social || `${u.first_name || ""} ${u.last_name || ""}`.trim() || u.email || "—";
}

function buildFilterUrl(params: Record<string, string>) {
  const qs = Object.entries(params).filter(([_, v]) => v).map(([k, v]) => `${k}=${v}`).join("&");
  return qs ? `/gestion/pedidos?${qs}` : "/gestion/pedidos";
}
---

<AdminLayout title="Pedidos - Admin Alcora" activeTab="pedidos">
  <div class="space-y-5">

    <!-- Header -->
    <div class="flex flex-col sm:flex-row sm:items-center justify-between gap-3">
      <div>
        <h1 class="text-2xl font-bold text-navy">Pedidos y Presupuestos</h1>
        <p class="text-text-muted text-sm mt-0.5">{total} resultado{total !== 1 ? "s" : ""}</p>
      </div>
    </div>

    <!-- Filtros tipo -->
    <div class="flex flex-wrap gap-2">
      {TIPOS.map((t) => (
        <a
          href={buildFilterUrl({ tipo: t.value, estado: estadoFilter })}
          data-astro-reload
          class:list={[
            "px-4 py-2 rounded-lg text-sm font-semibold transition-colors border",
            tipoFilter === t.value
              ? "bg-navy text-white border-navy"
              : "bg-white text-navy border-border hover:border-navy",
          ]}
        >
          {t.label}
        </a>
      ))}
    </div>

    <!-- Filtros estado -->
    <div class="flex flex-wrap gap-2">
      {ESTADOS.map((e) => (
        <a
          href={buildFilterUrl({ tipo: tipoFilter, estado: e.value })}
          data-astro-reload
          class:list={[
            "px-3 py-1.5 rounded-full text-xs font-medium transition-colors border",
            estadoFilter === e.value
              ? "bg-action text-white border-action"
              : "bg-white text-navy border-border hover:border-action hover:text-action",
          ]}
        >
          {e.label}
        </a>
      ))}
    </div>

    <!-- Table -->
    <div class="bg-white border border-border rounded-xl overflow-hidden">
      {pedidos.length > 0 ? (
        <>
          <div class="overflow-x-auto">
            <table class="w-full text-sm">
              <thead>
                <tr class="bg-bg-light border-b border-border text-xs text-text-muted uppercase tracking-wide">
                  <th class="text-left px-4 py-3 font-semibold">N.°</th>
                  <th class="text-left px-4 py-3 font-semibold">Tipo</th>
                  <th class="text-left px-4 py-3 font-semibold">Cliente</th>
                  <th class="text-left px-4 py-3 font-semibold">Fecha</th>
                  <th class="text-left px-4 py-3 font-semibold">Estado</th>
                  <th class="text-left px-4 py-3 font-semibold">Pago</th>
                  <th class="text-right px-4 py-3 font-semibold">Total</th>
                  <th class="px-4 py-3"></th>
                </tr>
              </thead>
              <tbody class="divide-y divide-border">
                {pedidos.map((p) => (
                  <tr class="hover:bg-bg-light transition-colors">
                    <td class="px-4 py-3 font-semibold text-navy">#{p.id}</td>
                    <td class="px-4 py-3">
                      <span class={`px-2 py-0.5 rounded-full text-xs font-medium ${
                        p.tipo === "presupuesto" ? "bg-amber-100 text-amber-800" : "bg-blue-100 text-blue-800"
                      }`}>
                        {p.tipo === "presupuesto" ? "Presupuesto" : "Pedido"}
                      </span>
                    </td>
                    <td class="px-4 py-3">
                      <div class="font-medium text-navy">{clienteNombre(p)}</div>
                      {p.user_created?.email && <div class="text-xs text-text-muted">{p.user_created.email}</div>}
                    </td>
                    <td class="px-4 py-3 text-text-muted whitespace-nowrap">{formatDate(p.date_created)}</td>
                    <td class="px-4 py-3">
                      <span class={`px-2 py-0.5 rounded-full text-xs font-medium ${estadoColorMap[p.estado] || "bg-gray-100 text-gray-700"}`}>
                        {estadoLabelMap[p.estado] || p.estado}
                      </span>
                    </td>
                    <td class="px-4 py-3 text-text-muted text-xs capitalize">
                      {p.tipo === "presupuesto" ? "—" :
                       p.metodo_pago === "transferencia" ? "Transferencia" :
                       p.metodo_pago === "pendiente" ? "Pendiente" : "—"}
                    </td>
                    <td class="px-4 py-3 text-right font-semibold text-navy">{Number(p.total || 0).toFixed(2)} €</td>
                    <td class="px-4 py-3 text-right">
                      <a href={`/gestion/pedidos/${p.id}`} data-astro-reload class="text-action hover:underline text-xs font-medium">
                        Ver →
                      </a>
                    </td>
                  </tr>
                ))}
              </tbody>
            </table>
          </div>

          {/* Pagination */}
          {totalPages > 1 && (
            <div class="flex items-center justify-between px-4 py-3 border-t border-border bg-bg-light">
              <p class="text-xs text-text-muted">Pagina {page} de {totalPages}</p>
              <div class="flex gap-2">
                {page > 1 && (
                  <a href={buildFilterUrl({ tipo: tipoFilter, estado: estadoFilter, page: String(page - 1) })}
                     data-astro-reload class="px-3 py-1.5 text-xs border border-border rounded-lg hover:bg-white transition-colors">
                    ← Anterior
                  </a>
                )}
                {page < totalPages && (
                  <a href={buildFilterUrl({ tipo: tipoFilter, estado: estadoFilter, page: String(page + 1) })}
                     data-astro-reload class="px-3 py-1.5 text-xs border border-border rounded-lg hover:bg-white transition-colors">
                    Siguiente →
                  </a>
                )}
              </div>
            </div>
          )}
        </>
      ) : (
        <div class="py-16 text-center text-text-muted">
          <span class="material-icons text-4xl opacity-30 mb-3 block">shopping_bag</span>
          <p>No hay pedidos con este filtro</p>
        </div>
      )}
    </div>
  </div>
</AdminLayout>
